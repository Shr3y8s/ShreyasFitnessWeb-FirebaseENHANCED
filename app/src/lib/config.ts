/**
 * Site Configuration
 * Manages configuration for the application
 */

// Note: Marketing site URLs no longer needed - all pages are now Next.js routes
// Kept this file for future configuration needs

export const SITE_CONFIG = {
  // Add future configuration here as needed
};
